import './globals.css';

export const metadata = {
  title: 'Nevlemar',
  description: 'Nevlemar kennel — Pomeranian & Cvergsnaucer breeding',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
