# Left empty on purpose: browser will call /api/* which next.config.mjs
# proxies server-side to http://localhost:3001. This works for both
# localhost and external IP access without CORS issues.
INTERNAL_API_URL=http://localhost:3001
